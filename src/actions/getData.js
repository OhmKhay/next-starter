import { GET_DATA } from './actionTypes';

export const getData =  (keyword) => async dispatch => {
  
  
  try {
     //do something ...
  } catch (error) {
      console.log('here is get data error:', error)
  }

};